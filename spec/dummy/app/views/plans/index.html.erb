<%- model_class = Plan -%>
<div class="page-header">
  <h1><%=t '.title', default: model_class.model_name.human.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:date) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', default: t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @plans.each do |plan| %>
      <tr>
        <td><%= link_to plan.id, plan_path(plan) %></td>
        <td><%= plan.name %></td>
        <td><%= plan.date %></td>
        <td><%=l plan.created_at %></td>
        <td>
          <%= link_to t('.edit', default: t("helpers.links.edit")),
                      edit_plan_path(plan), class: 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', default: t("helpers.links.destroy")),
                      plan_path(plan),
                      method: :delete,
                      data: { confirm: t('.confirm', default: t("helpers.links.confirm", default: 'Are you sure?')) },
                      class: 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', default: t("helpers.links.new")), new_plan_path, class: 'btn btn-primary' %>

<div class="continer">
  <div class="row">
    <div class="col-md-3">
    </div>
    <div class="col-md-6">
      <%= button_tag 'helpers.submit.create', class: 'btn btn-primary' %>
      <%= l Time.now, format: :long %>
    </div>
  </div>
</div>
<%= 'This is fackin girls'%>

<br>
<%= 'hello' %>
<br>
<%= 'activerecord.models' %>
<br>
<%= t 'activerecord.models' %>
<br>
<%= 'foo.bar' %>
<br>
<%= 'hello'.translate_false %>
<br>
<%= 'hello'.html_safe %>
<br>
<%= 'apple'.io count: 1 %>
<br>
DateTime: <%= DateTime.now.io format: :long %>
<br>
Date: <%= Date.today.io %>
<br>
Time: <%= Time.now.io format: :short %>
<br>
<%= link_to '日本語', plans_path(locale: :ja), class: 'btn btn-warning' %>
<%= link_to 'English', plans_path(locale: :en), class: 'btn btn-success' %>

